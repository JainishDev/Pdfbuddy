[build]
  command = "npm run build"
  publish = "dist"

[functions]
  node_bundler = "esbuild"
  # Increase timeout for PDF processing (max 26s on Netlify Pro, 10s on free)
  # For large PDFs, pdf-parse can take longer than 10s
  included_files = ["src/**/*"]

[build.environment]
  NODE_VERSION = "22"
  # Increase function timeout to maximum (26 seconds on Pro plans)
  NETLIFY_FUNCTIONS_TIMEOUT = "26"

# --- Security & performance headers ---
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"
    X-XSS-Protection = "1; mode=block"

# Cache immutable JS/CSS assets for 1 year
[[headers]]
  for = "/_astro/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Cache fonts for 1 year
[[headers]]
  for = "/fonts/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Never cache HTML pages
[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

# Never cache API responses
[[headers]]
  for = "/api/*"
  [headers.values]
    Cache-Control = "no-store"
